import { MemType } from './memType';

export class Member {
    memberId: number;
    firstName: string;
    lastName: string;
    dob: string;
    email: string;
    username: string;
    password: string;
    memType: MemType;
}